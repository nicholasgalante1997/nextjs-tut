export const header = {
    fontFamily: 'Spartan, sans-serif',
    color: 'black',
    fontWeight: 800
}

export const container = {
    padding: 8,
    color: 'white',
    fontFamily: 'Spartan, sans-serif',
    fontWeight: 800
}

export const nav = {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    position: 'fixed',
    top: 0,
    right: 0,
    left: 0,
    minHeight: 60,
    minWidth: '100vw',
    backgroundColor: 'darkblue',
    alignContent: 'center'
}

export const body = {
    position: 'fixed',
    top: 60,
    fontFamily: 'Spartan, sans-serif',
    height: '90%',
    width: '100vw',
    color: 'black',
    fontWeight: 800,
    overflow: 'auto',
    marginBottom: 16
}

export const navItem = {
    marginTop: 20,
    textDecoration: 'underline solid 1px white',
}

export const form = {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'space-evenly',
    alignItems: 'center',
    minHeight: 200,
}

export const button = {
    width: 100,
    height: 30,
    borderRadius: 4,
    borderColor: 'black',
    backgroundColor: 'lightgreen',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    padding: 4,
    fontFamily: 'Spartan, sans-serif',
    color: 'black',
    fontWeight: '800',
    boxShadow: '2px 2px 4px gray'
}

export const twoCol = {
    display: 'flex',
    flexDirection: 'row',
    minWidth: '100vw',
    justifyContent: 'space-evenly',
    alignItems: 'center'
}

export const col = {
    display: 'flex',
    justifyContent: 'center',
    padding: 8,
    minWidth: '45vw'
}

export const formRow = {
    padding: 10,
    marginVertical: 8,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    minWidth: '40vw',
    alignSelf: 'center'
}
